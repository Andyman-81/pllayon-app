[phases.install]
cmds = ["npm install -g pnpm@latest --ignore-scripts && pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/api-server build"]

[start]
cmd = "pnpm --filter @workspace/api-server start"
